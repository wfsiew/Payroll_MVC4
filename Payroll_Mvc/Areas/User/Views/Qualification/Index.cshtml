@{
    ViewBag.dialog_title = "Qualifications";
}

<div class="page_title ui-widget-header ui-widget">Qualifications</div>
@{ Html.RenderPartial("_form"); }
<div style="margin-top: 10px">
    @{ Html.RenderPartial("_save_button"); }
</div>
@{ Html.RenderPartial("_alert_panel"); }
